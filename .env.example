# URL pública do site (usada em sitemap.xml, robots.txt, canonical e Open Graph).
# Opcional: na Vercel a URL é detectada automaticamente. Defina apenas se usar
# um domínio próprio (ex.: https://meusite.com.br).
NEXT_PUBLIC_SITE_URL=

# Base da API FIPE (opcional). Padrão: parallelum.
FIPE_BASE_URL=https://parallelum.com.br/fipe/api/v1/carros
